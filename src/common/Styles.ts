import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    height: "100%",
    fontSize: 40,
    fontWeight: "700",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
});
